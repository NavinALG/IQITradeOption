//using Mopsicus.InfiniteScroll;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class History : MonoBehaviour
{

    /*[SerializeField]
    private InfiniteScroll Scroll;

    [SerializeField]
    private int Count = 100;

    void Start()
    {

        // Scroll.InitData(Count);
      //  StartCoroutine(callingrect());
    }
    IEnumerator callingrect()
    {
        yield return new WaitForSeconds(2);
        Scroll.OnFill += OnFillItem;
        Scroll.OnHeight += OnHeightItem;
      //  Debug.Log("count=" + GameObject.Find("Canvas/MainUI/History/ScrollView/Viewport/Content").transform.childCount);
        Scroll.InitData(GameObject.Find("Canvas/MainUI/History/ScrollView/Viewport/Content").transform.childCount);
    }
    void OnFillItem(int index, GameObject item)
    {
      //  item.GetComponentInChildren<Text>().text = index.ToString();
    }

    int OnHeightItem(int index)
    {
        return 100;
    }

   
    public void HistoryTimer()
    {


        gameObject.SetActive(false);

    }*/
}
